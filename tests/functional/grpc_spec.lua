--- Legacy grpcurl command-building tests removed; gRPC runs via kulala-core (@grpc/grpc-js).
local h = require("test_helper")

describe("grpc", function()
  it("is implemented in kulala-core", function()
    pending("See packages/core parser and grpc tests in kulala-core")
  end)
end)
